export MINIO_ACCESS_KEY='1w122131' # Minio user name / access key
export MINIO_SECRET_KEY='132123' # Minio user password / secret key
export MINIO_SERVER='12321' # Minio server url in <hostname:port> format
export MINIO_BUCKET_NAME='12321' # Update with the bucket name
# Redis configuration
export REDIS_SERVER='' # Redis server address
export REDIS_PORT=6379
# DMS configuration
export DMS_SERVER_NAME='A.1' # DMS Server name / ID